{
  "name": "ps-extender",
  "version": "2.2.0",
  "description": "Practice School Extender for BITS Pilani",
  "private": "true",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "build": "tsc && vite build",
    "build:watch": "tsc && vite build --watch",
    "build:test": "tsc && vite build --config vite.test.config.js && cp -r test/StudentStationPreference.aspx_files demo/test/StudentStationPreference.aspx_files",
    "dev": "vite",
    "preview": "vite preview --config vite.test.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.28.0",
    "typescript": "^4.6.4",
    "vite": "^3.2.3"
  }
}
